# Robolectric ships one android-all jar per API level and does not have one for the app's
# targetSdk yet, so the level is pinned here instead of tracking targetSdk automatically.
# Nothing under test is version-dependent: these are Compose semantics assertions.
sdk=35
